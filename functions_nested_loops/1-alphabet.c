#include <stdio.h>
#include <unistd.h>
#include "main.h"

/**
 * main - prints _putchar, followed by a new line.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char c = "a";
while (c <= "z")
{
printf('%c', c)
c++;
}
putchar('\n')
putchar(" print_alphabet")
return (0);
}
